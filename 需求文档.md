# Sofle Choc Wireless ZMK 配置方案

## 项目概述

本配置方案基于 ZMK 固件，为 **Sofle Choc Wireless** 分体键盘提供完整的无线键盘配置支持。

### 硬件特性

| 特性 | 配置 |
|------|------|
| 主控芯片 | Nice Nano V2|
| 编码器 | 左右各一个旋钮 |
| 显示屏 | 左右各一个 4-pin OLED (SSD1306 128x32) |
| RGB灯 | 左右各30颗 WS2812 LED |
| 主从连接方式 | 蓝牙无线 |

---

---

## 功能特性

### 键盘图层

| 图层 | 名称 | 功能 |
|------|------|------|
| Layer 0 | DEFAULT | QWERTY 主布局，顶行含 RGB 调节快捷键 |
| Layer 1 | LOWER  | 符号键（RA/LS 修饰）、`=`、`-`、`` ` ``、PIPE 等 |
| Layer 2 | RAISE  | F1-F12、方向键、INS/HOME/END/BSLH |
| Layer 3 | ADJUST | 同时按住 LOWER+RAISE 触发：蓝牙配对 + RGB 调节 + Ext Power |
| Layer 4 | FN     | 媒体控制（播放/上下首/音量）、K_PREVIOUS/K_APP/K_NEXT |

Layer 0 顶行（左手侧第 1 行）目前用作 RGB 调节快捷键：
`ESC` `&rgb_ug RGB_TOG` `&rgb_ug RGB_BRI` `&rgb_ug RGB_HUI` `&rgb_ug RGB_SAI` `&rgb_ug RGB_EFF`

注：原始需求文档"将 layer 0 的左上角几个键设置成 oled 的调节按键"目前**仅覆盖 RGB 调节**（亮/暗、色相、饱和、效果），OLED 自身的开关/亮度调节需要通过 ADJUST layer 触发。

### 编码器功能

| 位置 | 默认功能 | Layer 3 功能 |
|------|----------|--------------|
| 左旋钮 | 音量增减 | 鼠标水平滚动 |
| 右旋钮 | 页面滚动 | 鼠标垂直滚动 |

### OLED 显示

- **主控侧**: 显示完整状态（图层、蓝牙、电量）
- **从控侧**: 显示与主控相同的动画
- 屏幕：SSD1306，I2C 地址 `0x3c`，128×32 像素
- I2C 节点：当前使用 `&pro_micro_i2c`（nice_nano_v2 ZMK 板级 dts 提供的 label）
- `prechargep = <0x22>`，开启 `segment-remap` + `com-invdir` + `com-sequential`

---

## 构建方法

### 方法一：GitHub Actions 云端构建

1. 将代码推送到 GitHub 仓库
2. 自动触发构建
3. 从 Actions 页面下载固件 `.uf2` 文件

> **板子名称格式说明**：在 build.yaml 中使用 `nice_nano_v2` 格式指定版本，本仓库已统一使用此名称。

### 构建产物

`build.yaml` 包含 5 个 build target，覆盖 standalone 与 USB 无线 dongle 两种模式：

| 模式 | 产物 | 说明 |
|------|------|------|
| Standalone | `sofle_left`  | left 为主控 (central)，right 为从机 (peripheral) |
| Standalone | `sofle_right` | 配对 right |
| Dongle    | `sofle_dongle` | 第三个 nice_nano_v2，作为 USB 桥 |
| Dongle    | `sofle_left_peripheral`  | left 在 dongle 模式下作 peripheral |
| Dongle    | `sofle_right_peripheral` | right 在 dongle 模式下作 peripheral |

Dongle 配置位于 `boards/shields/sofle_dongle/`，**无 display、无 encoder**（`CONFIG_ZMK_DISPLAY=n` / `CONFIG_EC11=n`）。

## ZMK Studio 支持

**当前状态：未启用。** 本仓库固定在 ZMK v0.3，而 ZMK Studio 仅在 v0.4+ 引入。`config/sofle_left.conf` 中已移除 `CONFIG_ZMK_STUDIO=y`。

### 为什么不升级到 v0.4+

- ZMK 仓库的 git tag 最高为 `v0.3.0`（CalVer 标签如 `2024.04.00` 未发布）
- 可选分支：`main`（不稳定，每天都在变）、`v0.3-branch`（仅维护 v0.3，不含 Studio）
- 把 `west.yml` 的 `revision` 指向 `main` 的 commit SHA 是唯一可用方案，但每次 ZMK 上游变化都需手动验证
- 历史尝试：`@v0.5.0` 标签不存在 → 报错 `reference to workflow should be either a valid branch, tag, or commit`

### 启用 ZMK Studio 的前置条件

1. 等 ZMK 上游发布稳定的 CalVer tag（如 `2024.04.00`），或
2. 自行把 `west.yml` 的 `revision` 改为 `main` 的某个 commit SHA，并同步更新 `.github/workflows/build.yml` 的 `uses:` 到同一个 SHA
3. 在 `config/sofle_left.conf` 重新加上 `CONFIG_ZMK_STUDIO=y` 和 `CONFIG_ZMK_USB=y`
4. 验证 keymap 兼容（v0.4+ `&kp RA(...)` / `&inc_dec_kp` 等可能需要调整）
5. 验证 nice_nano_v2 board 在新版本仍受支持

---

## 版本记录

### 已知可工作版本：`7f07560`（基线）

- **commit hash**：`7f07560d51456adbb90c2ee74295cd2ab000c526`
- **分支**：`oled-rgb`
- **commit message**：`Add ZMK Studio support and RGB controls on layer 0`
- **来源**：在 `07137f4`（"Replace with new project from zbrabe/sofle"）的基础上，新增 ZMK Studio + USB HID 支持、并在 layer 0 顶行加入 RGB 调节快捷键

### 当前 HEAD：基于 `7f07560` + 实测引脚终版

- 在 `7f07560` 之上把三个 overlay（`sofle_left.overlay` / `sofle_right.overlay` / `sofle_panda.overlay`）的引脚改为实测值：
  - RGB SPI MOSI：`NRF_PSEL(SPIM_MOSI, 0, 6)` —— **P0.06**（实测确认）
  - OLED I2C SDA：`NRF_PSEL(TWIM_SDA, 0, 17)` —— **P0.17**
  - OLED I2C SCL：`NRF_PSEL(TWIM_SCL, 0, 20)` —— **P0.20**
- OLED 通过 `&i2c0` + 自定义 pinctrl 驱动（不使用 `&pro_micro_i2c`）

### ZMK 固件版本：保持 v0.3

- `config/west.yml` 的 `defaults.revision` 保持 `v0.3`
- `.github/workflows/build.yml` 的 `uses:` 保持 `v0.3.0`
- 此前曾尝试升级到 v0.5，但上游 git tag 不存在（`@v0.5.0` 报 `reference to workflow should be either a valid branch, tag, or commit`），已回退

#### 本版本关键配置

**板子 / Shield**

- `board: nice_nano_v2`
- shields：`sofle_left`、`sofle_right`、`sofle_dongle`（本地 `boards/shields/sofle_dongle/`）
- 本地 `zephyr/module.yml` 声明 `board_root: .`（让 west 找到本地 shield）

**RGB（WS2812）**

- `chain-length = <30>`（单边 30 颗 WS2812）
- SPI 引脚：`NRF_PSEL(SPIM_MOSI, 0, 6)` —— **P0.06**（实测确认）
- `spi-max-frequency = <4000000>`
- `color-mapping = <GREEN RED BLUE>`（GRB 顺序）
- `ZMK_RGB_UNDERGLOW_ON_START=y` —— 上电即亮
- `BRT_START=50`，`SAT_START=100`，`HUE_START=200`

**OLED（SSD1306 128×32）**

- I2C 地址 `0x3c`
- 节点 label：`&pro_micro_i2c`（这是让 OLED 在本版本能正常工作的关键；后续 commit 改成 `&i2c1` 后 OLED 不再显示）
- `width=128`、`height=32`、`multiplex-ratio=31`
- 开启：`segment-remap`、`com-invdir`、`com-sequential`
- `prechargep = <0x22>`

**Keymap**

- 5 层：DEFAULT / LOWER / RAISE / ADJUST / FN
- ADJUST 由 conditional_layers 触发（同时按住 LOWER+RAISE）
- Layer 0 顶行左 6 键为 RGB 调节：`ESC` `RGB_TOG` `RGB_BRI` `RGB_HUI` `RGB_SAI` `RGB_EFF`
- ADJUST layer 包含完整 RGB 调节面板 + 蓝牙切换 + Ext Power

**Dongle 模式**

- `boards/shields/sofle_dongle/` 三个文件齐备：`Kconfig.shield` / `Kconfig.defconfig` / `sofle_dongle.{conf,overlay}`
- dongle overlay 引用 `#include <layouts/josefadamcik/sofle.dtsi>`（使用 ZMK 仓库内置的 sofle 物理布局与 matrix-transform）

**Conf 关键开关**

- `CONFIG_ZMK_DISPLAY=y`
- `CONFIG_ZMK_STUDIO=y`（仅 left）
- `CONFIG_ZMK_USB=y`（仅 left）
- `CONFIG_ZMK_SLEEP=y`，`CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000`（30 分钟）
- `CONFIG_BT_CTLR_TX_PWR_PLUS_8=y`
- `CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y`
- `CONFIG_ZMK_RGB_UNDERGLOW=y` + `CONFIG_WS2812_STRIP=y` + `CONFIG_LED_STRIP=y`
- `CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n`（关 ext power 联动，让 OLED 在 RGB off 时仍可工作）

#### 回退到此版本的方式

```bash
git checkout 7f07560
# 或者：
git reset --hard 7f07560
```

### 历史回退记录

- `9e3d83c`（"Add custom OLED modules and status screen support"）—— 把 OLED 改用 `&pro_micro_i2c` 之外的方式（mctechnology17/zmk-nice-oled 等），**导致 OLED 不显示**，不应回退到此。
- 后续曾尝试把 SPI pin 改回 P0.08、chain-length 改回 30；当前 HEAD（`75db442` 之后）已采用 P0.08 + chain-length 58。

---

## 参考资源

- [ZMK 官方文档](https://zmk.dev/docs)
- [Sofle Choc Wireless 开源项目](https://github.com/db-ok/SofleChocWireless)
- [Nice Nano V2 文档](https://nicekeyboards.com/docs/nice-nano)
- [Sofle Keyboard 官方文档](https://josefadamcik.github.io/SofleKeyboard/)

---

## 本轮问题排查：右侧 OLED 不亮 + RGB 完全不亮

### 现状快照

| 项目 | 状态 |
|---|---|
| 左侧 OLED | 亮（固件正常时） |
| 右侧 OLED | **不亮** |
| RGB underglow（左右） | **完全不亮** |
| 使用模式 | standalone（`sofle_left.uf2` + `sofle_right.uf2`） |
| 参考 PCB | [db-ok/SofleChocWireless](https://github.com/db-ok/SofleChocWireless)（引脚一致） |

### 已做的修复（不依赖硬件测量）

- `chain-length = <58>` → `<30>`（左右各 30 颗，58 是合计理论上限 + 魔法数 2 的误算）
- 清理三个 overlay 文件头残留的"Panda Choc v1"注释
- panda overlay 头部加上"保留、未来用"说明

### 硬件测量结果（2026-07-11 终版）

| 信号 | 实测接到 nice_nano 哪个 pin | 与原假设对比 |
|---|---|---|
| RGB DIN | **P0.06** | ❌ 原 overlay 假设 P0.08，错的（实测 P0.08 不是 RGB data） |
| OLED SDA | **P0.17** | ❌ 原 overlay 假设 P0.06（`&pro_micro_i2c` 默认），错的 |
| OLED SCL | **P0.20** | ❌ 原 overlay 假设 P0.08，错的 |

**P0.08 用途**：原理图标注为 data 引脚，但实测**不是 RGB 的 DIN**，具体功能待查（推测是 OLED 复位/未连接/其他信号）。RGB 已确认在 P0.06。

### 真实根因（与最初假设完全不同）

最初担心 OLED SCL 和 RGB MOSI 撞 P0.08 — **实测后不存在冲突**。

真正的根因是：**overlay 用了 `&pro_micro_i2c` 节点，这个 label 在 nice_nano_v2 板级 dts 里默认走 P0.06/P0.08，但 OLED 实际接在 P0.17/P0.20**。结果就是固件往错误的 I²C 脚发数据，屏幕当然不亮。

RGB 配置本身没错，但因 OLED 节点占用同一个 i2c 控制器（如果走 `&pro_micro_i2c` 同节点竞争）或编译时 dtc 报错连 RGB 一起失效，需要分离。

### 已做的二次修复（基于实测）

把 `&pro_micro_i2c` 节点替换为 `&i2c0` + 自定义 pinctrl：

```
&pinctrl {
    i2c0_default: i2c0_default {
        group1 {
            psels = <NRF_PSEL(TWIM_SDA, 0, 17)>, <NRF_PSEL(TWIM_SCL, 0, 20)>;
        };
    };
    i2c0_sleep: i2c0_sleep { ... same with low-power-enable ... };
};

&i2c0 {
    status = "okay";
    compatible = "nordic,nrf-twim";
    pinctrl-0 = <&i2c0_default>;
    pinctrl-1 = <&i2c0_sleep>;
    pinctrl-names = "default", "sleep";
    clock-frequency = <100000>;

    oled: ssd1306@3c { ... SSD1306 配置不变 ... };
};
```

三个 overlay（`sofle_left.overlay` / `sofle_right.overlay` / `sofle_panda.overlay`）全部统一改成这个写法。

为什么用 `&i2c0` 而不是继续用 `&pro_micro_i2c`：
- `pro_micro_i2c` 是个 label，底层还是 i2c1；改它的 pinctrl 需要覆盖 board-level dts，脆弱
- `i2c0` 在 nice_nano_v2 上默认脚是 P0.04/P0.05，没人用，直接覆盖干净
- nRF52840 的两个 I²C 外设寄存器独立，效果无差

### 接下来还需要确认的事

#### I²C 上拉电阻（最关键！）

P0.17/P0.20 是 nice_nano 焊盘上的**非常规脚位**，板上**没有现成的上拉电阻**。OLED 模块自身通常自带 4.7kΩ 上拉，但如果你的屏幕模块上拉被去掉了，或者用的是裸屏，就需要外加。

**请量一下**：

1. 万用表电阻档，**红表笔接 nice_nano 3V3 焊盘，黑表笔接 P0.17**
2. 读数应该是 **1kΩ ~ 10kΩ**（常见 4.7kΩ = 上拉存在）
3. 同样量 P0.20 到 3V3

| 信号 | 电阻读数 | 是否有上拉？ |
|---|---|---|
| P0.17 → 3V3 | **?**Ω | 是 / 否 |
| P0.20 → 3V3 | **?**Ω | 是 / 否 |

**如果两个都显示 "OL"（无穷大）**：需要在屏幕模块的 SDA/SCL 到 VCC 之间各焊一个 4.7kΩ 电阻（或在 nice_nano 焊盘上飞线加）。

#### RGB 电源

虽然固件配置对了，但 RGB 还是不亮，可能的原因：

1. nice_nano 上 P0.08 是不是能输出 SPI MOSI（是的，spi3-mosi 默认就是 P0.08）
2. 灯条电源 VCC 是否接好（WS2812 通常 5V，需要从 nice_nano 的 VUSB 取电，不能从 3V3 取）
3. DIN 数据线方向：灯条 DIN 有方向性，接反了不亮

如果上面都 OK，刷完固件 RGB 应该亮。

### 验收 checklist（刷固件后逐项勾）

- [ ] **右侧 OLED**：通电 2 秒内显示 layer 名 / 电量 / 蓝牙状态
- [ ] **左侧 OLED**：未引入新故障，仍正常显示
- [ ] **RGB**：上电即亮（HUE=200, SAT=100, BRT=50 默认值，蓝色偏绿）
- [ ] **蓝牙配对**：`sofle_right.uf2` 烧录后能与 `sofle_left.uf2` 配对
- [ ] **编译产物**：从 GitHub Actions 下载的 5 个 .uf2 文件都能成功烧录
- [ ] **Sleep 行为**：30 分钟无输入进 sleep（`CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000`）

### 调试下一步（如果刷完还有问题）

| 现象 | 下一步 |
|---|---|
| OLED 仍不亮 | I²C 上拉缺失 → 加 4.7kΩ；或 OLED 屏坏了 → 换屏 |
| OLED 亮但花屏/抖 | I²C 时钟不稳 → 把 `clock-frequency` 从 `<100000>` 降到 `<40000>` |
| RGB 仍不亮 | 检查 5V 供电；用逻辑分析仪看 P0.08 是否有 SPI 时钟波形 |
| 编译报错 `&i2c0 not found` | 退回 `&i2c1` 节点（需要重新定义 pinctrl label） |
| 编译报错 `&pro_micro_i2c not found` | 忽略，已经不再引用这个 label |