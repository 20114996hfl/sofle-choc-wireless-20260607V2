# Sofle Keymap 修改需求模板

> 填写说明：确定的内容直接填写；不确定的内容写“待定”；不清楚 ZMK 键名时，可以用“左手第二排第 3 键”等物理位置描述。

## 【总原则】

1. 任何需求与现有配置不一致时，必须先向我说明：
   - 我的需求是什么；
   - 现有配置是什么；
   - 两者冲突在哪里；
   - 有哪些可选处理方案。
2. 未经我明确确认，不得覆盖、删除或调整现有功能。
3. 没有明确提到的按键、层、Combo、Tap Dance、宏、旋钮和其他配置保持不变。
4. 如果需求在 ZMK 中无法直接实现，或可能引发误触、编译问题，先说明原因并提供替代方案。
5. 修改前先读取并分析当前项目中的 keymap 及相关配置文件。
6. 修改完成后提供修改摘要、键位差异、风险说明和验证结果。
7. 需求整理阶段只维护本需求文档，不修改 `config/sofle.keymap`。
8. 已于 2026-08-17 在对话中明确确认进入实施阶段；本次依据本文档统一修改 keymap，并暂不处理括号/符号双击及 Unicode 输出。

---

## 【目标】

主要使用场景：

- [X] 中文输入
- [X] 编程
- [X] 办公
- [ ] 游戏
- [ ] 其他：

整体目标：

- 优化日常中文输入和编程操作。
- 尽量减少手指和手腕移动。
- 尽量支持单手完成对应层的操作。
- 其他：

不能接受的情况：

- [ ] 改变现有字母布局
- [ ] 删除现有功能
- [ ] 改变已有层编号
- [ ] 改变已有层切换方式
- [ ] 为新功能占用已有按键
- [ ] Combo 在快速中文输入时频繁误触
- [ ] 其他：

---

## 【基础信息】

- 键盘：Sofle
- 固件：ZMK
- 操作系统：主要使用 Windows；macOS / Linux 最好支持，不支持也可以接受
- 中文输入法：微软拼音或搜狗输入法
- 英文键盘布局：QWERTY / 其他：
- 当前 keymap：请直接读取项目中的配置文件
- 现有布局：
  - [ ] 必须保留
  - [ ] 可以局部调整，但需要先确认
  - [X] 可以重构，但需要先提供方案
- 键盘是否有显示屏：  是
- 键盘是否有两个旋钮： 是
- 左右半边是否均为无线： 是
- 其他硬件信息：

---

## 【各层总览】

| 层编号 | 层名称 | 主要用途 | 主要操作侧 | 进入方式 | 确定程度 |
|---|---|---|---|---|---|
| 0 | 默认层 | 日常字母输入 | 双手 | 开机默认 | 已确定 |
| 1 | F/快捷键层 | F 键及常用快捷键 | 左手 | 左侧切层键 | 部分确定 |
| 2 | 数字层 | 数字小键盘及运算符号 | 右手 | 右侧切层键 | 部分确定 |
| 3 | ADJUST / ZMK 设置层 | 蓝牙与 RGB 设置 | 左手 | 同时按住第 1、2 层切换键 | 已确定 |
| 4 |  |  |  |  | 待定 |

层切换的通用要求：

- 第 1 层需要能够仅使用左手进入并操作。
- 第 2 层需要能够仅使用右手进入并操作。
- 已确认：需求布局中明确留空且没有承担切层、现有功能保留或待定用途的按键，最终使用 `&none`，避免误触。
- 例外：层切换键、标为“待定”的位置，以及与当前 keymap 现有功能发生冲突的位置，必须先确认，不能直接改成 `&none`。

---

## 【第 0 层：默认层】

### 作用

- 日常中英文输入。
- 保留当前主要字母布局。
- 其他：

### 层布局

请直接在方格内填写预期键位；未修改的位置写“保持现状”。不知道键名时，可以填写自然语言功能。

```text
左手：
┌────┬────┬────┬────┬────┬────┐
│ ESC│ 1  │ 2  │ 3  │ 4  │ 5  │
├────┼────┼────┼────┼────┼────┤
│ tab│ Q  │ W  │ E  │ R  │ T  │
├────┼────┼────┼────┼────┼────┤
│    │ A  │ S  │ D  │ F  │ G  │
├────┼────┼────┼────┼────┼────┤
│ shift│ Z  │ X  │ C  │ V  │ B  │
└────┴────┴────┴────┴────┴────┘

左手拇指区，从外到内：
[  ctrl  ] [   win    ] [alt] [     space     ] [  按下时切换到1层   ]

右手：
┌────┬────┬────┬────┬────┬────┐
│ 6  │  7 │  8 │  9 │  0 │退格│
├────┼────┼────┼────┼────┼────┤
│ Y  │ U  │ I  │ O  │ P  │  \ │
├────┼────┼────┼────┼────┼────┤
│ H  │ J  │ K  │ L  │ ;  │  " │
├────┼────┼────┼────┼────┼────┤
│ N  │ M  │ ,  │ .  │ /  │    │
└────┴────┴────┴────┴────┴────┘

右手拇指区，从内到外：
[  按下时切换到2层   ] [enter] [   ctrl  ] [        ] [          ]
```

- 已确认：Backspace 正下方的物理按键是反斜杠 `\` 键。
- 当前 keymap 在该位置写的是 `LBKT`（`\[`），与需求文档不一致；最终实施时应改为 `BSLH`（`\`），但当前阶段不修改 keymap。

### 第 0 层旋钮

左侧旋钮：

- 逆时针：方向键左
- 顺时针：方向键右
- 按下：待定
- 建议但未确认：`Home`，与左右方向旋转组成行内导航；也可以保持 `&none`，避免误按。

右侧旋钮：

- 逆时针：方向键上
- 顺时针：方向键下
- 按下：待定
- 建议但未确认：`End`，与左旋钮按下的 `Home` 形成“左侧到开头、右侧到结尾”的记忆关系；也可以保持 `&none`。

- 已确认第 0 层必须使用上述左右、上下方向键方案，不能用于音量或 PageUp / PageDown。
- 当前 keymap 的第 0 层旋钮功能与此处不一致；最终实施时按需求文档修改。

---

## 【第 1 层：F 键及快捷键层】

### 作用

- 提供 F1～F12。
- 提供复制、粘贴等常用快捷键。
- 切层键位于左侧，主要功能能够由左手单手操作。
- 必须保留现有的两个 F12。
- 其他：

### 进入和退出方式

- 第 1 层切换键的位置：
- 切换方式：按住生效
- 如果切层键还有短按功能：
- 是否允许从其他层进入：允许

### F 键排列

- 最顶排的预期排列：
- 第一个 F12 的物理位置：
- 第二个 F12 的物理位置：
- 两个 F12：功能相同 / 功能不同，区别是：
- 两个 F12：必须原位保留 / 可以移动但必须保留两个 / 修改前确认

### 快捷键

| 物理位置或当前按键 | 输出的快捷键 | 备注 |
|---|---|---|
|  | ZMK 内置复制键码 | 不使用宏 |
|  | ZMK 内置粘贴键码 | 不使用宏 |
|  | ZMK 内置剪切键码 | 不使用宏 |
|  | ZMK 内置撤销键码 | 不使用宏 |
|  |  |  |

### 层布局

未安排的位置请明确填写“保持现状”“透明”或“禁用”。

```text
左手：
┌────┬────┬────┬────┬────┬────┐
│F12 │ F1 │ F2 │ F3 │ F4 │ F5 │
├────┼────┼────┼────┼────┼────┤
│ ~  │NONE│NONE│win+e│win+r │NONE│
├────┼────┼────┼────┼────┼────┤
│caps│ctrl+a│ctrl+s│NONE│ctrl+f│NONE│
├────┼────┼────┼────┼────┼────┤
│NONE│ctrl+z│ 剪切│ 复制│ 粘贴 │NONE│
└────┴────┴────┴────┴────┴────┘

左手拇指区，从外到内：
[          ] [          ] [          ] [          ] [          ]

右手：
┌────────┬────────┬────────┬────────┬────────┬────────┐
│   F6   │   F7   │   F8   │   F9   │  F10   │  F11   │
├────────┼────────┼────────┼────────┼────────┼────────┤
│ Insert │  Home  │ PageUp │  NONE  │   ↑    │  F12   │
├────────┼────────┼────────┼────────┼────────┼────────┤
│ Delete │  End   │PageDown│   ←    │   ↓    │   →    │
├────────┼────────┼────────┼────────┼────────┼────────┤
│  NONE  │  NONE  │  NONE  │  NONE  │  NONE  │  NONE  │
└────────┴────────┴────────┴────────┴────────┴────────┘

右手拇指区，从内到外：
[          ] [          ] [          ] [          ] [          ]
```

### 第 1 层旋钮

| 旋钮 | 逆时针 | 顺时针 | 按下 |
|---|---|---|---|
| 左侧 | 音量降低 | 音量增加 | 静音 |
| 右侧 | PageUp | PageDown | Home（已确认） |

- 已确认：音量及翻页功能放在第 1 层，不放在第 0 层。
- 第 1 层旋钮按键应显式配置，不能依赖第 0 层透明继承。
- 已确认：第 1 层右侧保留传统六键导航区，并提供实体方向键。
- 导航区使用 `Insert / Home / PageUp` 和 `Delete / End / PageDown` 的两行排列。
- 方向键使用倒 T 形排列；除两个区域、F6～F12 外的明确空位使用 `&none`。

---

## 【第 2 层：数字小键盘及运算符号层】

### 作用

- 提供数字小键盘。
- 提供常用运算符号。
- 切层键位于右侧，主要功能能够由右手单手操作。
- 其他：

### 进入和退出方式

- 第 2 层切换键的位置：
- 切换方式：按住生效 / 单击切换 / 短按与长按双功能 / 保持现状 / 其他：
- 如果切层键还有短按功能：
- 是否允许从其他层进入：

### 已填写的数字小键盘草稿

> 保留你已经填写的内容，作为下面完整层布局的参考。

```text
右手预期数字区：


[     +    ] [    7     ] [    8     ] [    9     ] [          ]
[     -    ] [    4     ] [    5     ] [    6     ] [          ]
[     *    ] [    1     ] [    2     ] [    3     ] [          ]
[     /    ] [    数字小键盘的enter     ][    0     ] [    .     ]
[          ] [          ] [          ] [          ] [          ]
[    切层键     ] [          ] [          ] [          ] [          ]
```

### 完整层布局

请将上面的数字区安排到对应方格中；其他位置写“保持现状”“透明”或“禁用”。

```text
左手：
┌──────┬──────┬──────┬──────┬──────┬──────┐
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
└──────┴──────┴──────┴──────┴──────┴──────┘

左手拇指区，从外到内：
[          ] [          ] [          ] [          ] [          ]


右手： 这些都是数字小键盘上的
┌────────┬────────┬────────┬────────┬────────┬────────┐
│   +    │   7    │   8    │   9    │  NONE  │  NONE  │
├────────┼────────┼────────┼────────┼────────┼────────┤
│   -    │   4    │   5    │   6    │  NONE  │  NONE  │
├────────┼────────┼────────┼────────┼────────┼────────┤
│   *    │   1    │   2    │   3    │  NONE  │  NONE  │
├────────┼────────┼────────┼────────┼────────┼────────┤
│   /    │   =    │   0    │   .    │KP Enter│NumLock │
└────────┴────────┴────────┴────────┴────────┴────────┘

右手拇指区，从内到外：
[          ] [          ] [          ] [          ] [          ]
```

数字键类型：普通主键盘数字 / 小键盘数字 KP_0～KP_9 / 不确定，请先解释区别

### 运算符号

| 符号或功能 | 期望位置 | 输出内容 | 备注 |
|---|---|---|---|
| 加号 |  | `+` |  |
| 减号 |  | `-` |  |
| 乘号 |  | `*` |  |
| 除号 |  | `/` |  |
| 等号 |  | `=` |  |
| 小数点 |  | `.` |  |
| Enter |  |  |  |
| Backspace |  |  |  |
| 其他 |  |  |  |

### 第 2 层旋钮

| 旋钮 | 逆时针 | 顺时针 | 按下 |
|---|---|---|---|
| 左侧 | 待定 | 待定 | 待定 |
| 右侧 | 待定 | 待定 | 待定 |

---

## 【第 3 层】

- 层名称：ADJUST / ZMK 设置层
- 主要作用：蓝牙连接与 RGB 设置
- 主要操作侧：左手
- 进入方式：同时按住第 1 层和第 2 层的切层键
  - 第 0 层：按住两颗切层键，使 `LOWER` 和 `RAISE` 同时激活。
  - 第 1 层：在第 2 层切换键的对应位置绑定 `&mo RAISE`。
  - 第 2 层：在第 1 层切换键的对应位置绑定 `&mo LOWER`。
  - 由现有 conditional layer 在 `LOWER + RAISE` 同时激活时自动进入 `ADJUST`。
- 退出方式：松开任意一颗切层键后退出第 3 层；两颗都松开后回到第 0 层
- 当前配置：已有 `LOWER + RAISE → ADJUST` 条件层；修改前需修正或确认第 0 层的 `&kp LOWER`
- 预期按键如下；未修改的位置写“保持现状”“透明”或“禁用”：

### 本轮第 3 层目标布局（2026-08-24，已确认实施）

- 第 3 层的名称、用途、进入方式和退出方式保持不变。
- 左手前三排采用下面的完整蓝牙与 RGB 控制布局。
- 左手第四排继续标记为“待定”，本轮不得修改这些按键。
- 已核对 `config/sofle.keymap`：左手前三排及 `LOWER + RAISE → ADJUST` 条件层已与本节一致；本轮重新生成最新固件供刷入，不改动待定键位。
- 右侧 OLED 以用户实机确认可正常显示的提交 `1443515` 为基线：使用 ZMK 自带的 Sofle SSD1306 节点，仅在 overlay 中覆盖实测 I²C 引脚，不重复定义 OLED 节点；恢复右侧电量百分比显示。

建议将每一种 RGB 调节的“增加/前进”放在上排，将“减少/后退”放在正下方。开启和关闭也使用上下两个键：

```text
左手：
┌────────┬────────┬────────┬────────┬────────┬────────┐
│ BT_CLR │  BT1   │  BT2   │  BT3   │  BT4   │  BT5   │
├────────┼────────┼────────┼────────┼────────┼────────┤
│ RGB开  │ 模式+  │ 速度+  │ 亮度+  │ 色相+  │饱和度+ │
├────────┼────────┼────────┼────────┼────────┼────────┤
│ RGB关  │ 模式-  │ 速度-  │ 亮度-  │ 色相-  │饱和度- │
├────────┼────────┼────────┼────────┼────────┼────────┤
│  待定  │  待定  │  待定  │  待定  │  待定  │  待定  │
└────────┴────────┴────────┴────────┴────────┴────────┘

左手拇指区，从外到内：
[          ] [          ] [          ] [          ] [          ]

右手：
┌──────┬──────┬──────┬──────┬──────┬──────┐
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ NONE │ NONE │ NONE │ NONE │ NONE │ NONE │
└──────┴──────┴──────┴──────┴──────┴──────┘

右手拇指区，从内到外：
[          ] [          ] [          ] [          ] [          ]
```
- 对应 ZMK behavior：
  - RGB 开 / 关：`RGB_ON` / `RGB_OFF`
  - 模式 + / -：`RGB_EFF` / `RGB_EFR`
  - 速度 + / -：`RGB_SPI` / `RGB_SPD`
  - 亮度 + / -：`RGB_BRI` / `RGB_BRD`
  - 色相 + / -：`RGB_HUI` / `RGB_HUD`
  - 饱和度 + / -：`RGB_SAI` / `RGB_SAD`
- 已确认：第 3 层右手按键区域全部使用 `&none`，不保留原有的右侧 RGB 重复键。
- 左手最后一排：待定；当前阶段不得修改。
- 旋钮行为：待定；当前阶段不得修改。

---

## 【第 4 层】

- 层名称：
- 主要作用：待定
- 主要操作侧：
- 进入方式：
- 退出方式：
- 当前配置：完全保留 / 暂时不修改 / 读取后向我说明
- 预期按键如下；未修改的位置写“保持现状”“透明”或“禁用”：

```text
左手：
┌────┬────┬────┬────┬────┬────┐
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
└────┴────┴────┴────┴────┴────┘

左手拇指区，从外到内：
[          ] [          ] [          ] [          ] [          ]

右手：
┌────┬────┬────┬────┬────┬────┐
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┤
│    │    │    │    │    │    │
└────┴────┴────┴────┴────┴────┘

右手拇指区，从内到外：
[          ] [          ] [          ] [          ] [          ]
```
- 旋钮行为：待定；当前阶段不得修改。

---`

## 【组合键 Combo】

### 通用规则

- 生效层：默认仅第 0 层；另有说明时分别指定。
- 同时按键判定时间：初始使用 `40 ms`；如果真机测试仍有滚键误触，再降低为 `35 ms`。
- 是否允许滚动按下触发：
- 触发后是否屏蔽原按键输出：
- 快速输入时：优先避免误触 / 优先容易触发 / 按不同 Combo 分别配置
- Combo 与长按键或层切换键冲突时：必须先确认。
- 已确认：大部分需要 Shift 的英文标点，不要求用户实际按住 Shift；优先使用两个相邻物理键组成 Combo。
- 实现时优先绑定 ZMK 已有的符号键码或带修饰符键码，不为普通符号创建宏。

### 规则 1：上下排字母组合输出 Shift+数字符号

目标：仅在第 0 层，通过上下两排的对应字母组合，产生数字行 `Shift+1` 至 `Shift+8` 的效果。原有 `Shift+9` 和 `Shift+0` Combo 已确认删除，小括号统一使用规则 2 的纵向括号 Combo。

| Combo | 预期按键输出 | 预期显示字符 | 是否启用 | 备注 |
|---|---|---|---|---|
| Q + A | `Shift+1` | `!` 或 `！`： | [x] |  |
| W + S | `Shift+2` | `@`： | [x] |  |
| E + D | `Shift+3` | `#`： | [x] |  |
| R + F | `Shift+4` | `$` 或 `￥`： | [x] |  |
| T + G | `Shift+5` | `%`： | [x] |  |
| Y + H | `Shift+6` | `^`： | [x] |  |
| U + J | `Shift+7` | `&`： | [x] |  |
| I + K | `Shift+8` | `*`： | [x] |  |

- 固件应当：使用 ZMK 内置符号键码或等价的带 Shift 键码；用户只按 Combo，不需要亲自按 Shift。
- 生效层：仅第 0 层
- 按住是否允许重复输出：
- 中文输入法产生全角符号时：接受输入法结果 / 希望强制指定字符 / 先讨论实现方案

### 规则 2：括号、连接号和下划线

这里的字母仅表示第 0 层对应按键的物理位置。所有组合仅在第 0 层生效。

| 组合按键 | 单击输出 | 快速双击输出 | 备注 |
|---|---|---|---|
| 数字 3 + 数字 4 | `{` | 无 | 左大括号；位于 E、R 两列的数字行 |
| E + R | `[` | `【` | 左中括号：半角 / 全角 |
| D + F | `(` | `（` | 左小括号：半角 / 全角 |
| C + V | `<` | 无 | 左尖括号 |
| 数字 7 + 数字 8 | `}` | 无 | 右大括号；对应 U、I 两列的数字行 |
| U + I | `]` | `】` | 右中括号：半角 / 全角 |
| J + K | `)` | `）` | 右小括号：半角 / 全角；已确认使用 `J+K` |
| M + , | `>` | 无 | 右尖括号；与左侧 `C+V` 镜像 |
| G + H | `-` | `——` | 双击输出两个 U+2014 中文破折号，不是 `--` |
| V + B | `_` | 无 | 下划线 |

括号 Combo 从上到下固定使用同一对物理列：

```text
左侧 E/R 两列：  数字3+数字4 → {    E+R → [    D+F → (    C+V → <
右侧 U/I 两列：  数字7+数字8 → }    U+I → ]    J+K → )    M+, → >
```

- 已确认删除规则 1 中重复的 `O+L → (` 和 `P+; → )`，小括号只使用此处的纵向括号 Combo。

- 全角字符必须直接输出指定 Unicode 字符，不依赖输入法的中英文或标点状态。
- 主要目标系统：Windows。
- 需要兼容的中文输入法：微软拼音、搜狗输入法。
- Unicode 实现方案：暂缓；目前不引入 `zmk-unicode`、WinCompose，也不修改 Windows 设置。
- 暂不实现的输出：`【`、`】`、`（`、`）`、`——`；保留需求供以后启用。
- Combo 判定时间：初始使用 `40 ms`；如果真机测试仍有滚键误触，再降低为 `35 ms`。
- 输入前空闲时间：建议先使用 `80–100 ms`，降低连续打字时的误触。
- Tap Dance 双击间隔：建议先使用 `200 ms`，真机测试后调整。
- 按住是否允许重复：待确认。

### 尚未分配位置的传统 Shift 符号

以下符号也应优先通过两个相邻物理键 Combo 输入，但具体组合位置尚未确认，当前不得擅自分配：

| 符号 | 对应传统按键 | 组合位置 | 状态 |
|---|---|---|---|
| `~` | Shift + Backtick | 待定 | 待规划 |
| `+` | `Shift + =` | 待定 | 待规划 |
| `|` | Shift + Backslash | 待定 | 待规划 |
| `:` | `Shift + ;` | 待定 | 待规划 |
| `"` | `Shift + '` | 待定 | 待规划 |
| `?` | `Shift + /` | 待定 | 待规划 |

### 其他 Combo

| 组合按键 | 输出内容 | 生效层 | 长按重复 | 备注 |
|---|---|---|---|---|
| Backspace + 它正下方的 `\` 键 | `Delete` | 仅第 0 层 | 不适用 | 两颗键单独按时保持原功能；Backspace 必须允许快速连续退格，不能使用 Tap Dance |
|  |  |  |  |  |

---

## 【Tap Dance 键】

### 总体目标

我主要使用中文输入法，希望部分标点能够：

- 单击：
- 快速双击：
- 长按：
- 三击：

“从中文符号切换到英文符号”具体是指：

- [ ] 单击发送普通标点，由中文输入法产生中文符号；双击发送输入法的中英文标点切换快捷键
- [ ] 单击输出中文全角字符，双击输出英文半角字符
- [ ] 第一次输出中文符号，第二次删除并替换成英文符号
- [ ] 双击直接输出两次英文符号
- [x] 其他：括号类 Combo 单击直接输出半角字符，双击直接输出指定全角 Unicode 字符；不依赖输入法状态

输入法信息：

- 输入法名称：Windows 微软拼音 / 搜狗输入法
- 切换中英文标点的快捷键：
- 是否需要兼容多个系统或多个输入法：主要保证 Windows；兼容微软拼音和搜狗输入法

### Tap Dance 时间参数

- 双击最长间隔：使用现有值 / 请推荐 / 指定为：_200_ ms
- 单击后是否需要立即输出：
- 是否允许长按重复：
- 超时后的预期行为：
- 快速连续输入标点时：优先连续输入 / 优先识别双击 / 按不同按键配置

### Tap Dance 按键表

| 物理按键 | 单击 | 双击 | 长按 | 三击 | 生效层 |
|---|---|---|---|---|---|
| 逗号键 | `，` | `,` |  |  | 第 0 层 |
| 句号键 | `。` | `.` |  |  | 第 0 层 |
| 分号键 | `；` | `;` |  |  | 第 0 层 |
| 冒号键 | `：` | `:` |  |  | 第 0 层 |
| 问号键 | `？` | `?` |  |  | 第 0 层 |
| 感叹号键 | `！` | `!` |  |  | 第 0 层 |
| 引号 |  | `"` |  |  |  |
| Combo：E + R | `[` | `【` |  |  | 第 0 层 |
| Combo：D + F | `(` | `（` |  |  | 第 0 层 |
| Combo：U + I | `]` | `】` |  |  | 第 0 层 |
| Combo：J + K | `)` | `）` |  |  | 第 0 层 |
| Combo：G + H | `-` | `——` |  |  | 第 0 层 |

---

## 【普通短按/长按双功能键】

| 物理按键 | 短按 | 长按 | 生效层 | 长按判定时间 |
|---|---|---|---|---|
|  |  |  |  |  |
|  |  |  |  |  |

滚键或快速输入要求：优先普通按键 / 优先容易触发长按修饰键 / 按不同按键配置

---

## 【快捷键：不使用宏】

- 已确认：复制、粘贴、剪切、撤销等常见操作不创建自定义宏。
- 优先使用 ZMK 已有的快捷键/编辑键码；如果目标系统兼容性不足，再使用单个直接组合绑定，例如 `&kp LC(C)`，仍不定义宏 behavior。
- 实施前应核对 Windows 对所选 ZMK 编辑键码的兼容性。

| 名称 | 触发按键 | 输出或动作 | 生效层 | 备注 |
|---|---|---|---|---|
| 复制 | 第 1 层对应位置 | ZMK 内置复制键码或直接 `Ctrl+C` 绑定 | 第 1 层 | 禁止使用宏 |
| 粘贴 | 第 1 层对应位置 | ZMK 内置粘贴键码或直接 `Ctrl+V` 绑定 | 第 1 层 | 禁止使用宏 |
| 剪切 | 第 1 层对应位置 | ZMK 内置剪切键码或直接 `Ctrl+X` 绑定 | 第 1 层 | 禁止使用宏 |
| 撤销 | 第 1 层对应位置 | ZMK 内置撤销键码或直接 `Ctrl+Z` 绑定 | 第 1 层 | 禁止使用宏 |
|  |  |  |  |  |

---

## 【蓝牙、系统及其他功能】

需要保留或增加的功能：

- [x] 蓝牙配置选择
- [x] 蓝牙清除
- [ ] USB/蓝牙输出切换
- [ ] Bootloader
- [ ] 键盘重启
- [ ] Caps Word
- [ ] 截图
- [x] 音量控制
- [ ] 媒体播放
- [ ] 屏幕亮度
- [x] RGB
- [ ] 其他：

这些功能的位置：不允许改变 / 可以改变但需要先确认 / 可以重新规划

---

## 【修改流程要求】

### 第一步：只分析，不修改

先读取当前项目，然后提供：

1. 当前实际存在的层及名称；
2. 每一层的完整键位布局；
3. 所有层切换键的位置和方式；
4. 所有 Combo；
5. 所有 Tap Dance；
6. 所有宏和自定义 behavior；
7. 每一层的旋钮行为；
8. 两个 F12 的实际位置；
9. 当前配置与本需求的冲突；
10. 本需求中存在歧义或 ZMK 实现风险的地方。

### 第二步：确认方案

在我确认之前不要修改文件。将计划分为：

- 可以直接实现且不影响现有配置的项目；
- 与现有配置冲突的项目；
- 需要我补充信息的项目；
- 有多种实现方式、需要我选择的项目。

### 第三步：执行修改

只有得到我的明确确认后才能修改。

我的确认内容：

> 已于 2026-08-17 在对话中确认：依据需求文档修改配置并推送 GitHub 构建；括号/符号双击暂不处理。

### 第四步：验证和交付

- [ ] 检查 ZMK/Devicetree 语法
- [ ] 执行项目现有的编译或验证方式
- [ ] 展示最终各层布局
- [ ] 列出所有新增或修改的 Combo
- [ ] 列出所有 Tap Dance
- [ ] 列出所有时间参数
- [ ] 说明未实现或需要真机验证的部分

---

## 【补充说明】

在这里填写任何未被上述分类覆盖的需求：
